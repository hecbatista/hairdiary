import { NavigationProp } from "@react-navigation/native";
import { View } from "react-native";

export default function ResetPasswordScreen({ navigation }: { navigation: NavigationProp<any>}) {
    return(
        <View></View>
    );
}